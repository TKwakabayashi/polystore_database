# --env-file を付けずに docker compose を叩いたときの既定データセット。
# 実際に使うときは env/<dataset>.env を明示するのが確実:
#   docker compose --env-file env/openalex.env up -d
#
# ここは env/ldbc_sf1.env の最小サブセット。メモリ等も含めた完全な設定は
# env/ldbc_sf1.env 側にあるので、値を変えるならそちらを直すこと。
DATASET=ldbc_sf1
NEO4J_IMAGE=neo4j:5
NEO4J_DATABASE=neo4j
